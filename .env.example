# Google Cloud API key with Chrome UX Report API + PageSpeed Insights API enabled.
# Application restrictions must be None, or server-to-server calls get a 403.
CRUX_API_KEY=

# Optional: only if PageSpeed uses a different key than CrUX.
# PSI_API_KEY=
